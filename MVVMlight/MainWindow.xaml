<Window x:Class="MVVMlight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMlight"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="20">
        <TextBox DataContext="{Binding Main,
    Source={StaticResource Locator}}"
                 Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 10" />
        <Button Content="Click Me" Command="{Binding UpdateTextCommand}" />

        <Button DataContext="{Binding Set1,
    Source={StaticResource Locator}}" Content="Click Me"
                Command="{Binding UpdateTextCommand1}" />
    </StackPanel>
</Window>